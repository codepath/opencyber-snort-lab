# Run-smoke config for the Snort lab (consumed by ../_smoke-test.sh, rubric TECH-4).
PROBE_CMD='node ~/ftp_folder/attack.js "http://localhost:8888/general/reports.txt"'
PROBE_EXPECT='Status: 200'
# Retired product name — must not survive anywhere, including baked blobs.
BANNED_EXTRA='Fairly|Oddparent|Northwind'
